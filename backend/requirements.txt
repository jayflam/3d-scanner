# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic-settings>=2.6.0
python-multipart>=0.0.12

# Azure OpenAI
openai>=1.55.0

# TripoSR dependencies
torch>=2.1.0
omegaconf==2.3.0
Pillow>=10.1.0
einops>=0.7.0
transformers>=4.35.0
trimesh>=4.0.5
rembg[gpu]
huggingface-hub
imageio[ffmpeg]
xatlas>=0.0.9
moderngl>=5.10.0
numpy>=2.0

# torchmcubes must be installed from git:
#   pip install git+https://github.com/tatsy/torchmcubes.git

# Database
sqlalchemy[asyncio]>=2.0.25
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
alembic>=1.13.0

# Task queue
celery[redis]>=5.3.6
redis>=5.0.0

# Azure Blob Storage
azure-storage-blob>=12.19.0
azure-identity>=1.15.0

# Video processing
ffmpeg-python>=0.2.0

# PDF generation
weasyprint==60.2
tinycss2==1.2.1
pydyf==0.8.0

# Gaussian Splatting dependencies
# nerfstudio (ns-train, ns-export, splatfacto) installed in Dockerfile after torch
# colmap installed via apt in Dockerfile

# Testing
pytest>=7.0.0
pytest-asyncio>=0.23.0
httpx>=0.25.0
